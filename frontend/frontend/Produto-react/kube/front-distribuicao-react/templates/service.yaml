apiVersion: v1
kind: Service
metadata:
  name: {{ include "front-pedido-direto-react.name" . }}
  labels:
    {{- include "front-pedido-direto-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "front-pedido-direto-react.selectorLabels" . | nindent 4 }}
