apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "front-pedido-direto-react.name" . }}-test-connection"
  labels:
    {{- include "front-pedido-direto-react.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "front-pedido-direto-react.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
